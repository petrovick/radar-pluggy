## 1. Pré-requisitos externos (fora de código, bloqueantes)

- [x] 1.1 ~~Provisionar banco Aiven de staging~~ — decisão do usuário: reaproveitar o mesmo banco físico (`oplab_radar`, mesmo Aiven) que `oplab-radar-api` já usa neste ambiente, em vez de provisionar um banco novo. `.railway/railway.ts` referencia `DATABASES: '${{oplab-radar-api.DATABASES}}'` — Railway resolve o valor real internamente, nunca em texto puro no repo ou nas mãos de quem aplica. Verificado: `railway config plan` aceita a referência sem erro.
- [x] 1.2 Ambiente `staging` existe no projeto Railway `oplab-radar` — **não é um ambiente novo**: o `production` existente foi renomeado (mesmo ID), decisão explícita do usuário ciente de que `oplab-radar-api`/`oplab-radar-front` (já em produção real, domínio próprio) passam a compartilhar ambiente com `radar-pluggy`, sem isolamento. Confirmado via `list-services`/`environment-status` do MCP Railway: os dois serviços seguem `online` no mesmo ambiente.
- [x] 1.3 Criar um Railway project token escopado ao ambiente `staging` e cadastrá-lo como secret `RAILWAY_TOKEN` — feito como secret do **GitHub Environment** `staging` (Settings → Environments → staging → Environment secrets), não secret de repositório; os workflows foram ajustados para declarar `environment: staging` nos jobs que o usam, senão o secret chega vazio.

## 2. Código: ambiente `staging` reconhecido pela migration

- [x] 2.1 Adicionar um getter `staging` em `sequelize.config.cjs`, byte a byte igual ao `production` existente (lê `DATABASES`, mesmo tratamento de TLS/CA do Aiven) — verificar: rodar localmente `DATABASES='<json-de-teste>' node node_modules/sequelize-cli/lib/sequelize db:migrate --status --env staging` não lança mais "Environment staging not found".
- [x] 2.2 Rodar a suíte de testes existente (`npm test`) e confirmar que nenhum teste de `sequelize.config.cjs`/migration quebrou — verificar: suíte verde.

## 3. Infraestrutura declarativa (`.railway/railway.ts`)

- [x] 3.1 Escrever `.railway/railway.ts` com `export const partial = "radar-pluggy"` como primeira declaração (o projeto `oplab-radar` tem `oplab-radar-api`/`oplab-radar-front` em outro repositório — sem `partial`, `config apply` reconciliaria o projeto inteiro e poderia remover os dois), seguido de `service("radar-pluggy", …)`: `source: image("ghcr.io/petrovick/radar-pluggy:staging")`, `healthcheck: "/healthcheck"`, `preDeploy: "node node_modules/sequelize-cli/lib/sequelize db:migrate --env staging"`, `env` com `NODE_ENV`, `CONFIG` (`preserve()` — `pluggy.credentialEncryptionKey` mora dentro do `CONFIG`, não em env var solta, por decisão do usuário), `DATABASES` (referência `${{oplab-radar-api.DATABASES}}`) — verificado: `railway link` + `railway config plan` local (precisou instalar o pacote npm `railway`, SDK oficial exigido por `railway/iac` — adicionado como devDependency) → `Plan: 1 to add, 0 to change, 0 to destroy — Create service radar-pluggy`. Confirma que `partial` protege `oplab-radar-api`/`oplab-radar-front`, mesmo compartilhando ambiente.
- [x] 3.2 `railway link` + `railway config apply --yes` rodado manualmente. Serviço `radar-pluggy` criado (id `47f44a70-b4d7-411f-b867-74c8ef4877e1`); `oplab-radar-api`/`oplab-radar-front` intocados (confirmado via `list-services`). Domínio customizado `radar-pluggy.petrovick.dev` anexado via MCP Railway (`generate-domain`) — pendente CNAME no DNS (`radar-pluggy` → `etrnwtjq.up.railway.app`, `DNS_RECORD_STATUS_REQUIRES_UPDATE`). `CONFIG` real setado via `set-variables` (`http.port`, `http.jwtSecret` gerado, `pluggy.webhookUrl` = `https://radar-pluggy.petrovick.dev/webhooks/pluggy`, `pluggy.credentialEncryptionKey` gerado — dentro do `CONFIG`, não em env var solta, por decisão do usuário). Deploy inicial falhou como esperado (imagem ainda não publicada) — verificado via `environment-status`, fica pra task 5.2.

## 4. Pipeline de CI/CD

- [x] 4.1 Criar `.github/workflows/deploy-staging.yml` com `permissions: contents: read, packages: write`: build da imagem (`Dockerfile`, estágio `runtime`) em push para a branch `staging` (default do repositório — **corrigido depois**: nasceu apontando pra `main` por engano meu, branch que este repositório não usa; sem isso o workflow nunca disparava), login no GHCR (`GITHUB_TOKEN`), push com as tags `:staging` e `:sha-<hash-curto>`, `concurrency: group: deploy-staging` (evita corrida entre execuções), passo final `railway redeploy --service radar-pluggy --yes` autenticado por `RAILWAY_TOKEN` — verificar: sintaxe do YAML válida (lint/`actionlint`); execução real fica pra task 5.
- [x] 4.2 Criar `.github/workflows/railway-config.yml`: job `plan` em `pull_request` (paths `.railway/**`) com `permissions: contents: read, pull-requests: write, actions: read` (comenta o plano no PR), job `apply` em merge na `main` com `permissions: contents: read, actions: read, pull-requests: read` (resolve qual PR mergeada gerou o plano), ambos usando a action `railwayapp/config@v1` com `RAILWAY_TOKEN` — verificar: sintaxe válida; o primeiro `plan` real roda no próprio PR deste change.

## 5. Primeiro deploy real e limpeza

- [ ] 5.1 Após o primeiro push da imagem pro GHCR, marcar o pacote `ghcr.io/petrovick/radar-pluggy` como público (GitHub → Package settings → Change visibility) — verificar: `docker pull ghcr.io/petrovick/radar-pluggy:staging` funciona sem login.
- [ ] 5.2 Confirmar que o serviço `staging` sobe de ponta a ponta: migration aplicada sem erro (log do `preDeploy`), healthcheck verde no dashboard, domínio responde — verificar: `curl` no domínio do serviço retorna 200 em `/healthcheck`.
- [x] 5.3 Remover `railway.json` do repositório — verificar: `git status` limpo e o job `docker` existente em `.github/workflows/ci.yml` (smoke test de build) continua verde sem esse arquivo.
